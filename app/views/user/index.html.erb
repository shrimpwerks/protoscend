<header>
  <h1 style="display:inline-block;">Users</h1>
  <div class="pull-right" style="margin-top: 20px;">
    <%= form_tag user_index_path, method: "get",
      class: "form-inline",
      style: "display:inline-block;" do %>

      <%= text_field_tag "search", params[:search], class: "form-control", autofocus: true %>
      <%= submit_tag "Search", class: "btn btn-primary" %>

    <% end %>
  </div>
</header>

<table class="table">
  <thead>
    <th><%= sortable "fname", "First Name" %></th>
    <th><%= sortable "lname", "Last Name" %></th>
    <th><%= sortable "email", "Email"  %></th>
    <th><%= sortable "user_level", "Access" %></th>
    <th><%= sortable "routes_count", "Routes Count" %></th>
  </thead>
  <tbody>
    <% @users.each do |user| %>
      <tr class="table__row" data-url="<%= user_path(user.id) %>">
        <td><%= highlight user.fname, params[:search] %></td>
        <td><%= highlight user.lname, params[:search] %></td>
        <td><%= highlight user.email, params[:search] %></td>
        <td><%= user.user_level %></td>
        <td><%= user.routes_count %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>

